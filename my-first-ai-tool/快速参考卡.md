# 🎯 快速参考卡

## 一分钟启动

```bash
# 1. 配置
cp .env.example .env
# 编辑 .env,填入 GITHUB_TOKEN

# 2. 安装
pip install -r requirements.txt

# 3. 运行
chainlit run app.py -w
```

## 核心命令

### 开发
```bash
# 启动 Web 界面
chainlit run app.py -w

# 命令行演示
python demo.py

# 快速测试
python demo.py test

# 交互模式
python demo.py chat

# 运行测试
pytest tests/ -v
```

### 调试
```bash
# 测试单个 Agent
python agents/travel_agent.py

# 检查工具
python -c "from tools.travel_tools import TravelToolsPlugin; print(TravelToolsPlugin().get_destinations())"
```

## 文件结构速查

```
my-first-ai-tool/
├── 📱 app.py                  # Chainlit Web 应用
├── 🎮 demo.py                 # 命令行演示
├── 🤖 agents/
│   └── travel_agent.py       # 主 Agent
├── 🛠️ tools/
│   └── travel_tools.py       # 工具定义
├── 📝 tests/
│   └── test_travel_agent.py  # 测试
└── 🔧 .env                    # 环境变量
```

## 核心概念

| 概念 | 文件 | 说明 |
|------|------|------|
| **Agent** | `agents/travel_agent.py` | 智能决策中心 |
| **Tools** | `tools/travel_tools.py` | 可调用的函数 |
| **Kernel** | `agents/` | Semantic Kernel 内核 |
| **Plugin** | `tools/` | 工具集合 |
| **Chat History** | `agents/` | 对话历史 |

## 关键代码片段

### 定义工具
```python
from semantic_kernel.functions import kernel_function
from typing import Annotated

@kernel_function(description="功能描述")
def my_tool(
    self,
    param: Annotated[str, "参数说明"]
) -> Annotated[str, "返回值说明"]:
    return result
```

### 创建 Agent
```python
from semantic_kernel import Kernel
from semantic_kernel.agents import ChatCompletionAgent

kernel = Kernel()
kernel.add_plugin(MyPlugin(), plugin_name="my_plugin")

agent = ChatCompletionAgent(
    kernel=kernel,
    name="My Agent",
    instructions="你的指令"
)
```

### 对话
```python
chat_history = ChatHistory()
chat_history.add_user_message("Hello")

async for content in agent.invoke_stream(chat_history):
    print(content.content)
```

## 常用工具函数

### 当前提供的工具

| 工具 | 功能 | 调用示例 |
|------|------|----------|
| `get_destinations` | 获取目的地列表 | "有哪些目的地?" |
| `check_destination_availability` | 查询可用性 | "巴黎可以预订吗?" |
| `recommend_destination` | 智能推荐 | "推荐预算中等的地方" |

## 配置选项

### 函数选择策略
```python
# 自动选择 (推荐)
FunctionChoiceBehavior.Auto()

# 必须使用工具
FunctionChoiceBehavior.Required()

# 不使用工具
FunctionChoiceBehavior.NoneInvoke()
```

### Agent 个性
```python
instructions="""
你是一个[角色]
你的风格: [描述]
你应该: [行为准则]
"""
```

## 测试检查清单

- [ ] Agent 初始化成功
- [ ] 工具能够正确调用
- [ ] 响应符合预期
- [ ] 多轮对话正常
- [ ] 错误处理得当

## 故障排除

### Agent 无响应
```bash
# 检查环境变量
cat .env

# 验证 Token
curl -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/user
```

### 依赖问题
```bash
# 清理重装
pip uninstall semantic-kernel -y
pip cache purge
pip install -r requirements.txt
```

### 端口占用
```bash
# 查看占用
lsof -i :8000

# 换端口
chainlit run app.py --port 8080
```

## 扩展示例

### 添加新工具
```python
# 1. 在 tools/travel_tools.py 添加
@kernel_function(description="获取天气")
def get_weather(self, city: str) -> str:
    return f"{city} 天气晴朗"

# 2. Agent 自动识别并使用
```

### 修改 Agent 风格
```python
# agents/travel_agent.py
instructions="""
你是一个幽默风趣的旅游助手 😄
使用轻松的语言,加入旅行小知识!
"""
```

### 添加新的数据
```python
# tools/travel_tools.py
destinations_db = {
    "beijing": {
        "available": True,
        "features": ["历史", "文化", "美食"],
        "weather": "四季分明"
    }
}
```

## 性能优化

```python
# 1. 缓存
from functools import lru_cache

@lru_cache(maxsize=100)
def cached_function(key):
    return expensive_operation(key)

# 2. 并行
import asyncio

results = await asyncio.gather(
    task1(), task2(), task3()
)

# 3. 流式输出
async for chunk in agent.invoke_stream(history):
    yield chunk
```

## 有用链接

- 📖 [完整指南](../前端AI业务工具开发实操指南.md)
- 🚀 [快速启动](快速启动指南.md)
- 🏗️ [架构文档](ARCHITECTURE.md)
- 📚 [Semantic Kernel 文档](https://learn.microsoft.com/semantic-kernel/)
- 💬 [Chainlit 文档](https://docs.chainlit.io/)

## 示例对话

### 查询类
- "有哪些目的地?"
- "巴黎可以预订吗?"
- "Tokyo 的详细信息"

### 推荐类
- "推荐一个预算中等的地方"
- "我喜欢海滩,有什么推荐?"
- "适合美食爱好者的目的地"

### 复杂问题
- "我想去欧洲,预算不高,喜欢文化,有什么推荐?"
- "对比一下巴黎和东京"
- "哪些地方现在可以预订?"

## 快捷键 (Cursor)

- `Cmd/Ctrl + K` - AI 编辑器
- `Cmd/Ctrl + P` - 快速打开文件
- `Cmd/Ctrl + Shift + F` - 全局搜索
- `Cmd/Ctrl + ~` - 打开终端

## 下一步

1. ✅ 运行基础应用
2. 🔨 添加自定义工具
3. 🎨 修改 Agent 个性
4. 🧪 编写测试用例
5. 🚀 部署到生产环境

---

**💡 提示**: 将这个文件保存到书签,随时查阅!
